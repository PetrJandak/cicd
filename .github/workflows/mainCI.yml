name: CI-MAIN

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:
  workflow_call:
jobs:
    villa:
       name: Villa Build FE
       uses: https://github.com/PetrJandak/villa/blob/main/.github/workflows/frontend.yml@main

